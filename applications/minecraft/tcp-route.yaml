apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: minecraft
  namespace: minecraft
spec:
  parentRefs:
    - name: external
      namespace: gateway
  rules:
    - backendRefs:
        - name: minecraft
          port: 25565

