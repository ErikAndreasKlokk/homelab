apiVersion: v1
kind: Service
metadata:
  name: claude-code
  namespace: claude-code
spec:
  type: ClusterIP
  selector:
    app: claude-code
  ports:
    - name: web
      port: 8080
      targetPort: 8080
      protocol: TCP
