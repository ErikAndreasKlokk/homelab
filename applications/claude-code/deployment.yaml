apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-code
  namespace: claude-code
  labels:
    app: claude-code
spec:
  replicas: 1
  # RWO local-path volume: never let a second pod mount it during a rollout.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: claude-code
  template:
    metadata:
      namespace: claude-code
      labels:
        app: claude-code
    spec:
      serviceAccountName: claude-code
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      # Clone claude-workspace into the PVC on first boot, then pull the project
      # repos. On later boots it fast-forwards; uncommitted work is preserved.
      initContainers:
        - name: bootstrap
          image: ghcr.io/erikandreasklokk/claude-workspace:main
          imagePullPolicy: Always
          env:
            # Optional: enables private-repo clone + push. Pod still starts without it.
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: claude-code-git
                  key: github-token
                  optional: true
          command:
            - bash
            - -lc
            - |
              set -e
              WS=/home/coder/workspace
              if [ ! -d "$WS/.git" ]; then
                echo "First boot — cloning claude-workspace..."
                git clone https://github.com/ErikAndreasKlokk/claude-workspace.git "$WS"
              fi
              cd "$WS"
              git pull --ff-only || echo "claude-workspace has local changes; skipping ff"
              bash bootstrap.sh
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: home
              mountPath: /home/coder
      containers:
        - name: code-server
          image: ghcr.io/erikandreasklokk/claude-workspace:main
          imagePullPolicy: Always
          # NOTE: root filesystem is intentionally writable (no
          # readOnlyRootFilesystem) — this is an interactive dev environment that
          # runs npm installs, builds, and Claude Code, matching the n8n pattern.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          args:
            - --bind-addr
            - 0.0.0.0:8080
            - /home/coder/workspace
          env:
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: claude-code-secret
                  key: code-server-password
          ports:
            - name: web
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 4Gi
          volumeMounts:
            - name: home
              mountPath: /home/coder
            - name: tmp
              mountPath: /tmp
        # Keeps a Remote Control session alive so the pod is reachable from the
        # Claude mobile app / claude.ai/code, surviving pod restarts. Reuses the
        # full-scope OAuth login persisted on the shared /home/coder PVC — that
        # login is a one-time manual step: exec into the code-server container,
        # run `claude` -> `/login`. If the login is missing this container just
        # crash-loops harmlessly until it exists.
        - name: remote-control
          image: ghcr.io/erikandreasklokk/claude-workspace:main
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          env:
            - name: HOME
              value: /home/coder
          command: ["bash", "-lc"]
          # `printf y` answers the one-time "Enable Remote Control?" consent;
          # `sleep infinity` holds stdin open so the session never EOFs and exits.
          # If claude does exit (e.g. >10 min network loss), the container's PID 1
          # exits and the kubelet restarts it — self-healing reconnect.
          args:
            - |
              cd /home/coder/workspace 2>/dev/null || cd /home/coder
              exec claude remote-control --name homelab < <(printf 'y\n'; sleep infinity)
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 4Gi
          volumeMounts:
            - name: home
              mountPath: /home/coder
      volumes:
        - name: home
          persistentVolumeClaim:
            claimName: claude-code-data
        - name: tmp
          emptyDir: {}
